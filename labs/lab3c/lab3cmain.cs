using System;

class Programm
{
	static void Main()
	{
		// Пример использования класса Circle
		Color circleColor=new Color(255,0,0);
		circle circle = new circle(5, circleColor);
		Console.WriteLine(circle);
	}
}

